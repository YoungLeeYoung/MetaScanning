# 复制为 .env 后填写（.env 已被 gitignore）
#
# GitHub token：强烈建议填。没有 token 时搜索接口限流 10 次/分钟，
# 一次完整扫描会非常慢（而且 snapshot 无法走 GraphQL 批量接口）。
# 只需要 public repo 的只读权限，classic token 不勾任何 scope 即可。
GITHUB_TOKEN=

# LLM 打分默认关闭。想开启时把 config 里的 llm.enabled 设为 true，然后填这里。
# 任何 OpenAI 兼容的 endpoint 都可以（OpenAI / DeepSeek / Together / 本地 vLLM / Ollama）。
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
METASCAN_MODEL=gpt-4o-mini
# 可选：给最终 top N 做深度分析用的更强模型，留空则复用上面那个
METASCAN_DEEP_MODEL=
